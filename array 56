using System;
using System.Collections.Concurrent;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
public class ejercicio_3
{
    public static void Main (String[] Args)
    {
    
    int [] enteros = {5,20,15,9,3,7,14};

    
     System.Console.WriteLine("indique un numero del 1 al 20: ");
     int numero=Convert.ToInt32(Console.ReadLine());

    bool encontrado = false;
     for (int i=0; i<enteros.Length; i++)
        {
            if (enteros[i] == numero)
            {
            System.Console.WriteLine($"el numero si esta dentro del array en el indice {i}" );
            encontrado = true;
            }
            
        }
        if (encontrado == false)
        {
            System.Console.WriteLine("el numero no esta en el array");
        }
    
    }
}
