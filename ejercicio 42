using System;

public class caso_1
{
    public static void Main(String[]Args)
    {
        for (int i = 0; i < 2; i++)
        {
            Console.WriteLine("Outer" + i);

        for (int j = 1; j <= 3; j++)
        {
        Console.WriteLine("inner" + j);
        }
        }
    }
}
